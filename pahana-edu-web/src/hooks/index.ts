/**
 * @fileoverview Central export point for all custom React hooks used throughout the application.
 *
 * This barrel file provides a convenient way to import hooks from a single location,
 * improving code organization and maintainability.
 *
 * @example
 * ```typescript
 * import { useTheme } from '@/hooks';
 * ```
 */
export { useTheme } from "./useTheme";
